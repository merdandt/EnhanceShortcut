# Voice Enhance Dependencies
#
# NOTE: torch/torchaudio, deepspeed and resemble-enhance itself are NOT listed
# here on purpose — they need pinned, ordered installs (see
# Dockerfile.voice_enhance and deploy_local.sh):
#   1. torch==2.4.1 / torchaudio==2.4.1  (cu121 index in Docker, CPU wheel locally;
#      torch>=2.6 breaks resemble-enhance's torch.load of the checkpoint)
#   2. deepspeed==0.15.4 --no-build-isolation  (imports torch at build time)
#   3. resemble-enhance @ git commit, --no-deps  (its dep list drags in
#      gradio/torchvision/etc. that inference never imports)

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# HTTP client for service-to-service communication
httpx>=0.25.0

# Environment management
python-dotenv>=1.0.0

# R2 (S3-compatible) storage
boto3>=1.35.0

# resemble-enhance runtime deps (the subset its inference path actually imports)
numpy==1.26.4
omegaconf>=2.3.0
soundfile>=0.12.1
scipy>=1.11.4
librosa>=0.10.1
matplotlib>=3.8.1
pandas>=2.1.3
rich>=13.7.0
tqdm>=4.66.1
# pandas.to_markdown() needs tabulate lazily during Enhancer.__init__.summarize()
tabulate>=0.8.10
